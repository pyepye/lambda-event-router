{
  "name": "@lambda-event-router/iot",
  "version": "0.1.0",
  "description": "IoT Core handler for lambda-event-router",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "tsc": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json --noEmit && tsup",
    "dev": "tsup --watch",
    "test": "#tsc --noEmit && vitest run",
    "test:check": "#vitest run",
    "test:watch": "vitest",
    "test:coverage": "#vitest run --coverage",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "aws",
    "lambda",
    "serverless"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@lambda-event-router/base": "workspace:*"
  },
  "devDependencies": {
    "@lambda-event-router/testing": "workspace:*",
    "@types/aws-lambda": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "tsup": "catalog:",
    "vitest": "catalog:"
  }
}
