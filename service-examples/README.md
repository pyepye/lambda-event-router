# Router Examples

A collection of working Lambda Event Router examples where each one can be deployed to AWS with a few commands.

Each example is self-contained and for a single AWS service. You can deploy the code by following the steps in the README.

They are intended to demonstrate how lambda-event-router works for each service. Examples may not follow best practices and may include unusual design choices, as the focus is on showing different features of the library.
