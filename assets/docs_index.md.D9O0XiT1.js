import{_ as t,o as a,c as r,ak as o}from"./chunks/framework.Ct7YXsF8.js";const m=JSON.parse('{"title":"Overview","description":"","frontmatter":{},"headers":[],"relativePath":"docs/index.md","filePath":"docs/index.md"}'),n={name:"docs/index.md"};function s(i,e,d,l,h,c){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h1><p>Lambda Event Router is a TypeScript framework for routing AWS Lambda events. You define routers for the AWS services you care about, register them with a <code>LambdaRouter</code> and the framework works out which router should handle each event.</p><p>This is really help when you want a single Lambda, or a set of Lambdas with shared code, to handle events from multiple sources. Instead of writing your own event detection logic, you declare filters and handlers and let the router do the matching.</p><p>We cover every AWS service that can <a href="https://docs.aws.amazon.com/lambda/latest/dg/lambda-services.html" target="_blank" rel="noreferrer">natively invoke a Lambda</a>. Check the <a href="/lambda-event-router/packages">packages page</a> for the full list. For AWS services without a direct Lambda trigger, you can use the <code>EventBridgeRouter</code> with CloudTrail events to cover those too.</p><h2 id="key-concepts" tabindex="-1">Key concepts <a class="header-anchor" href="#key-concepts" aria-label="Permalink to &quot;Key concepts&quot;">​</a></h2><p><strong>Standardised routing</strong> - The same pattern works across all AWS services / events. You define routes with filters and handlers regardless of whether you&#39;re dealing with SQS, API Gateway or DynamoDB Streams.</p><p><strong>Type-safe simplifed requests and responses</strong> - Each router gives you typed request objects with the data you actually need. Return types are enforced too, so you can&#39;t accidentally return the wrong shape for a given event source.</p><p><strong>Declarative filtering</strong> - Route events using service-specific data like ARN, message attributes, event name, HTTP method or detail type. You can also pass custom filter functions for anything more specific. See <a href="/lambda-event-router/docs/routing">routing and filtering</a> for more details.</p><p><strong>Schema validation</strong> - Validate parts of the event - body, attributes, path params - using any <a href="https://github.com/standard-schema/standard-schema" target="_blank" rel="noreferrer">Standard Schema</a> compatible library like Zod, Valibot or ArkType.</p><h2 id="why-lambda-event-router" tabindex="-1">Why Lambda Event Router? <a class="header-anchor" href="#why-lambda-event-router" aria-label="Permalink to &quot;Why Lambda Event Router?&quot;">​</a></h2><p>The framework handles event detection and routing so you can focus on your business logic. The API is consistent across every supported service, once you understand how the routing works for one event, you understand them all.</p><p>Everything is fully typed. Inline handlers get automatic type inference, schema validation feeds into your handler types and each router enforces the correct response shape for its event source.</p><h2 id="who-is-it-for" tabindex="-1">Who is it for? <a class="header-anchor" href="#who-is-it-for" aria-label="Permalink to &quot;Who is it for?&quot;">​</a></h2><p>Lambda Event Router is a good fit when:</p><ul><li>You use lambdas as your main compute and use different AWS services as event triggers</li><li>You want one Lambda handling multiple events from the same service. For example, a large number of APIGateway endpoints or several different SQS message types routed to different handlers.</li><li>You deal with events from multiple different AWS services and want a consistent pattern rather than bespoke event handling for each one.</li><li>You don&#39;t want hundreds of single-purpose Lambdas with sprawling deployments that are hard to manage</li></ul><h2 id="when-not-to-use-it" tabindex="-1">When not to use it <a class="header-anchor" href="#when-not-to-use-it" aria-label="Permalink to &quot;When not to use it&quot;">​</a></h2><p>If your Lambda handles a single event source in a single way, you probably don&#39;t need this. Here are some cases where it isn&#39;t the right fit:</p><ul><li><strong>Single event source with no filtering</strong> - Your Lambda receives events from one source and processes them all the same way. A plain handler function is simpler.</li><li><strong>Single-purpose Lambdas</strong> - Your Lambda does exactly one thing. Routing and filtering add indirection with no upside.</li><li><strong>HTTP-only Lambdas</strong> - Dedicated HTTP frameworks like Express, Hono and Fastify have richer ecosystems for middleware, auth and templating.</li><li><strong>Performance-critical Lambdas with simple logic</strong> - The router iterates through registered routers via <code>canHandleEvent</code> checks and applies middleware chains. For ultra-simple Lambdas where every millisecond counts, a direct handler avoids this overhead.</li><li><strong>One Lambda per event source</strong> - If you intentionally map one Lambda to one event source for isolated scaling, permissions or deployment, there&#39;s nothing to route between.</li></ul><h2 id="what-next" tabindex="-1">What next? <a class="header-anchor" href="#what-next" aria-label="Permalink to &quot;What next?&quot;">​</a></h2><p>Head to the <a href="/lambda-event-router/docs/quick-start">quick start</a> to get going.</p>',20)])])}const p=t(n,[["render",s]]);export{m as __pageData,p as default};
