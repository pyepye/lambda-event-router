import{_ as s,o as a,c as t,ak as i}from"./chunks/framework.DiNuf6zK.js";const k=JSON.parse('{"title":"Routers","description":"","frontmatter":{},"headers":[],"relativePath":"docs/routers.md","filePath":"docs/routers.md"}'),n={name:"docs/routers.md"};function r(o,e,h,d,l,p){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="routers" tabindex="-1">Routers <a class="header-anchor" href="#routers" aria-label="Permalink to &quot;Routers&quot;">​</a></h1><p>A router handles one AWS event source. It recognises the events that source sends and calls the handler you registered for them, giving it a typed object rather than a raw AWS envelope.</p><p>You create a router for each event source your Lambda receives, register routes on it, then hand it to <code>LambdaRouter</code>.</p><p>Matching happens at two levels. <code>LambdaRouter</code> works out which router should handle the event, then that router works out which of its routes should handle it. Everything else here assumes that split.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Handler } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aws-lambda&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { LambdaRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { snsRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./sns.js&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sqsRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./sqs.js&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Level one: LambdaRouter picks which router handles the event</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lambdaRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ routers: [sqsRouter, snsRouter] })</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lambdaRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sqs.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createSQSRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/sqs&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { processOrder } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./handlers/processOrder.js&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sqsRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createSQSRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Level two: the router picks which route handles the record</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sqsRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: { eventSourceArn: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ORDER_QUEUE_ARN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: processOrder,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>See <a href="/docs/quick-start">quick start</a> to build that up step by step.</p><h2 id="same-shape-everywhere" tabindex="-1">Same shape everywhere <a class="header-anchor" href="#same-shape-everywhere" aria-label="Permalink to &quot;Same shape everywhere&quot;">​</a></h2><p>Learn one router and the rest read in a similar way, so a Lambda handling four event sources does not mean four different mental models.</p><h3 id="exports" tabindex="-1">Exports <a class="header-anchor" href="#exports" aria-label="Permalink to &quot;Exports&quot;">​</a></h3><p>Every package exports the same three things, with <code>&lt;Source&gt;</code> standing in for the service.</p><table tabindex="0"><thead><tr><th>Name</th><th>What it is</th></tr></thead><tbody><tr><td><code>create&lt;Source&gt;Router(options)</code></td><td>Factory function, what you normally reach for</td></tr><tr><td><code>&lt;Source&gt;Router</code></td><td>The class behind it, if you would rather use <code>new</code></td></tr><tr><td><code>defineRoute</code></td><td>Builds a route with the types inferred from your schemas</td></tr></tbody></table><p>So Kinesis gives you <code>createKinesisRouter</code> and Firehose gives you <code>createFirehoseRouter</code>, without you having to look either up.</p><h3 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h3><p>These are the types you will actually import. Everything else is inferred, so you pass object literals rather than naming their types.</p><table tabindex="0"><thead><tr><th>Type</th><th>When you need it</th></tr></thead><tbody><tr><td><code>&lt;Source&gt;Request</code></td><td><a href="/docs/handlers#annotated-handlers">Annotating a handler</a> rather than inferring it</td></tr><tr><td><code>&lt;Source&gt;Response</code></td><td>Typing what that handler returns</td></tr><tr><td><code>&lt;Source&gt;Middleware</code></td><td>Writing <a href="/docs/middleware">middleware</a> in its own file</td></tr><tr><td><code>&lt;Source&gt;FilterInput</code></td><td>Writing a <a href="/docs/routing#custom"><code>custom</code></a> as a named function</td></tr></tbody></table><p>Each router page lists its full set, including the options and route definition types, for the times you want them.</p><h3 id="routes" tabindex="-1">Routes <a class="header-anchor" href="#routes" aria-label="Permalink to &quot;Routes&quot;">​</a></h3><p><code>route({ filters, handler })</code> works on every router, and every options object takes <code>middleware</code>. Only the service specific filter keys change.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SNS, filtering on the topic and a message attribute</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">snsRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    topicArn: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ORDER_TOPIC_ARN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    messageAttributes: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;OrderPlaced&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: onOrderPlaced,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DynamoDB Streams. Same call, and the keys come from the stream</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dynamoRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    eventName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;INSERT&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    eventSourceArn: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ORDER_TABLE_STREAM_ARN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: onOrderInserted,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p><a href="/docs/routing">Routing</a> covers all of this in full: the filter keys, <code>custom</code>, match order and where the schemas go. A few routers also add shorthand methods, so <code>dynamoRouter.insert()</code> is the route above with its <code>eventName</code> already filled in.</p><h3 id="naming-variations" tabindex="-1">Naming variations <a class="header-anchor" href="#naming-variations" aria-label="Permalink to &quot;Naming variations&quot;">​</a></h3><p>A few names vary, and they are worth knowing before you go looking for them:</p><ul><li><code>base</code> exports <code>defineEventRoute</code>, not <code>defineRoute</code></li><li>Packages holding more than one router may name it <code>define&lt;Router&gt;Route</code></li><li>Routers with per event variants name their requests after the event, so DynamoDB gives you <code>DynamoDBInsertRequest</code> and <code>DynamoDBRemoveRequest</code> rather than one <code>DynamoDBRequest</code></li><li>Some types come from <code>aws-lambda</code> rather than the package, usually the record type and <code>Context</code></li></ul><h2 id="choosing-a-router" tabindex="-1">Choosing a router <a class="header-anchor" href="#choosing-a-router" aria-label="Permalink to &quot;Choosing a router&quot;">​</a></h2><p>Every Lambda has a <code>LambdaRouter</code>, and it takes as many event routers as you need. What you choose is which of those to register on it.</p><p>Most services have a single router, so SQS gives you <code>SQSRouter</code> and nothing else to weigh up. A few send more than one kind of event and have a router for each, so API Gateway has <code>APIGatewayRouter</code>, <code>WebSocketRouter</code> and <code>LambdaAuthorizerRouter</code>. Choose per event source rather than per service.</p><table tabindex="0"><thead><tr><th>Your event source</th><th>What to use</th></tr></thead><tbody><tr><td>Triggers Lambda natively</td><td>The router for that service. See <a href="/packages">packages</a> for the full list</td></tr><tr><td>Has no Lambda trigger</td><td>Send CloudTrail to EventBridge, then <code>EventBridgeRouter</code></td></tr><tr><td>Has a custom envelope</td><td><code>EventRouter</code>. Covers EventBridge Scheduler and your own invoke payloads</td></tr></tbody></table><h2 id="requests-and-responses" tabindex="-1">Requests and responses <a class="header-anchor" href="#requests-and-responses" aria-label="Permalink to &quot;Requests and responses&quot;">​</a></h2><p>Both sides of a handler vary by event source: what it is given, and what it is allowed to send back. <a href="/docs/handlers">Handlers</a> covers the two together, and each router page documents its own fields.</p><p><strong>Read what a router&#39;s options do rather than going by the name.</strong> Options follow what each service supports, so the same name can mean different things in two packages. <code>batchItemFailures</code> on SQS reports individual record failures so only those get redelivered, while SNS has no mechanism for that and swallows the error instead.</p><h2 id="unique-routers" tabindex="-1">Unique routers <a class="header-anchor" href="#unique-routers" aria-label="Permalink to &quot;Unique routers&quot;">​</a></h2><p>Three routers are worth knowing about whatever your event source is. The rest are tied to one service and live on <a href="/packages">packages</a>.</p><h3 id="lambdarouter" tabindex="-1">LambdaRouter <a class="header-anchor" href="#lambdarouter" aria-label="Permalink to &quot;LambdaRouter&quot;">​</a></h3><p>The Lambda entry point. <code>lambdaRouter.handler()</code> is what you export, and every router gets registered on it.</p><p>It picks a router per event using each router&#39;s <code>canHandleEvent</code>, first match wins, and throws if nothing matches. A router that claims an event and has no route for it hands back rather than failing, so the next one gets a turn.</p><p>Middleware passed to <code>LambdaRouter</code> runs for every event, so tracing and logging belong here rather than being repeated on each router. It runs on the raw event and has a different signature to router and route middleware, since no router has been picked yet. See <a href="/docs/middleware">middleware</a>.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p><code>EventRouter</code> is sorted to the end of the list whatever order you register in, because its check is a catch-all. This means it can never take an event a dedicated router would have handled.</p></div><p>More on <a href="/routers/LambdaRouter">LambdaRouter</a>.</p><h3 id="eventrouter" tabindex="-1">EventRouter <a class="header-anchor" href="#eventrouter" aria-label="Permalink to &quot;EventRouter&quot;">​</a></h3><p>For events with a totally custom envelope. It lives in the base package, so there is nothing extra to install.</p><p>It refuses any event it recognises as a known AWS source, so adding an <code>EventRouter</code> cannot quietly break routing for the rest of your Lambda.</p><p><code>custom</code> is its only filter, and <code>eventSchema</code> gives you validation and typing. Routes are built with <code>defineEventRoute</code>.</p><p>More on <a href="/routers/EventRouter">EventRouter</a>.</p><h3 id="eventbridgerouter" tabindex="-1">EventBridgeRouter <a class="header-anchor" href="#eventbridgerouter" aria-label="Permalink to &quot;EventBridgeRouter&quot;">​</a></h3><p>Covers EventBridge buses, pipes and rules, and reaches services that cannot trigger Lambda at all by way of CloudTrail.</p><p>Both arrive as the same EventBridge envelope, so a CloudTrail event routes and types exactly like one you published yourself.</p><p>EventBridge Scheduler is the exception, as it delivers whatever payload you configured rather than an EventBridge envelope. Use <code>EventRouter</code> for those.</p><p>More on <a href="/routers/EventBridgeRouter">EventBridgeRouter</a>.</p>`,49)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
