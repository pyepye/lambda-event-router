import{C as t,o as n,c as h,ak as e,E as r}from"./chunks/framework.Ct7YXsF8.js";const k=JSON.parse('{"title":"LambdaAuthorizerRouter","description":"","frontmatter":{},"headers":[],"relativePath":"routers/LambdaAuthorizerRouter.md","filePath":"routers/LambdaAuthorizerRouter.md"}'),o={name:"routers/LambdaAuthorizerRouter.md"},c=Object.assign(o,{setup(d){const a=[{path:"index.ts",code:`import type { Handler } from 'aws-lambda'
import { LambdaRouter } from '@lambda-event-router/base'

import { authRouter } from './authorizer.js'

const lambdaRouter = new LambdaRouter({
  routers: [authRouter],
})

export const handler: Handler = lambdaRouter.handler()`},{path:"authorizer.ts",code:`import { createLambdaAuthorizerRouter, defineLambdaAuthorizerRoute } from '@lambda-event-router/apigateway'

import { authoriseRead, authoriseToken, authoriseWrite } from './handlers/auth.js'

export const authRouter = createLambdaAuthorizerRouter()

authRouter
  .token({ handler: authoriseToken })
  .request({ method: 'GET', handler: authoriseRead })
  .route(
    defineLambdaAuthorizerRoute({
      filters: {
        type: 'REQUEST',
        custom: ({ method }) => method !== 'GET',
      },
    }).handle(authoriseWrite),
  )`},{path:"handlers/auth.ts",code:`import type { APIGatewayAuthorizerResult } from 'aws-lambda'
import type {
  LambdaAuthorizerRequestRequest,
  LambdaAuthorizerTokenRequest,
} from '@lambda-event-router/apigateway'
import { Allow, Deny } from '@lambda-event-router/apigateway'
import { logger } from '@lambda-event-router/base'

import { apiKeys, users } from '../accounts.js'

export async function authoriseToken(request: LambdaAuthorizerTokenRequest): Promise<APIGatewayAuthorizerResult> {
  const { authorizationToken, resourceArn } = request

  const [scheme, token] = authorizationToken.split(' ')
  if (scheme !== 'Bearer' || !token) {
    logger.warn(\`Rejected a \${scheme} credential on \${resourceArn}\`)
    return Deny('anonymous', resourceArn)
  }

  const user = await users.fromToken(token)
  if (!user) {
    return Deny('anonymous', resourceArn)
  }

  return Allow(user.id, resourceArn, { tenantId: user.tenantId })
}

export async function authoriseRead(request: LambdaAuthorizerRequestRequest): Promise<APIGatewayAuthorizerResult> {
  const { headers, path, resourceArn } = request

  const client = await apiKeys.lookup(headers['x-api-key'])
  if (!client) {
    logger.warn(\`Rejected an API key reading \${path}\`)
    return Deny('anonymous', resourceArn)
  }

  return Allow(client.id, resourceArn, { tenantId: client.tenantId, plan: client.plan })
}

export async function authoriseWrite(request: LambdaAuthorizerRequestRequest): Promise<APIGatewayAuthorizerResult> {
  const { headers, method, path, resourceArn } = request

  const client = await apiKeys.lookup(headers['x-api-key'])
  if (!client) {
    logger.warn(\`Rejected an API key on \${method} \${path}\`)
    return Deny('anonymous', resourceArn)
  }

  // Reads are open to every plan, writes are not
  if (client.plan === 'free') {
    logger.info(\`Client \${client.id} is on the free plan and cannot \${method}\`)
    return Deny(client.id, resourceArn)
  }

  return Allow(client.id, resourceArn, { tenantId: client.tenantId, plan: client.plan })
}`},{path:"accounts.ts",code:`interface User {
  id: string
  tenantId: string
}

interface Client {
  id: string
  tenantId: string
  plan: 'free' | 'pro'
}

export const users = {
  async fromToken(token: string): Promise<User | undefined> {
    // Verify the JWT and load whoever it belongs to
    return { id: 'user-123', tenantId: 'acme' }
  },
}

export const apiKeys = {
  async lookup(key: string | undefined): Promise<Client | undefined> {
    if (!key) return undefined

    // Look the key up in whatever holds them
    return { id: 'client-456', tenantId: 'acme', plan: 'pro' }
  },
}`}];return(l,s)=>{const i=t("CodeFileViewer");return n(),h("div",null,[s[0]||(s[0]=e(`<h1 id="lambdaauthorizerrouter" tabindex="-1">LambdaAuthorizerRouter <a class="header-anchor" href="#lambdaauthorizerrouter" aria-label="Permalink to &quot;LambdaAuthorizerRouter&quot;">​</a></h1><p><code>LambdaAuthorizerRouter</code> routes Amazon API Gateway Lambda authorizer events to handlers, one authorization request per invocation.</p><p>Your handler decides whether a caller gets through and returns the IAM policy API Gateway acts on. One API can have several authorizers pointing at the same function, so routes match on which authorizer sent the event and on the method being called.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @lambda-event-router/apigateway</span></span></code></pre></div><p><code>@lambda-event-router/base</code> comes along as a dependency, so you do not need to install it yourself.</p><h2 id="create-the-router" tabindex="-1">Create the router <a class="header-anchor" href="#create-the-router" aria-label="Permalink to &quot;Create the router&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createLambdaAuthorizerRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { withTiming } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./middleware/withTiming&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> authRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createLambdaAuthorizerRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  middleware: [withTiming],  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p><code>middleware</code> is the only option and it can be left out. An authorizer runs before API Gateway builds a response, so there is no CORS to answer here, and everything else this router does is set on the routes. See <a href="#middleware">Middleware</a>.</p><h2 id="register-routes" tabindex="-1">Register routes <a class="header-anchor" href="#register-routes" aria-label="Permalink to &quot;Register routes&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;REQUEST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;POST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  middleware: [withAudit],  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseWrite,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p><code>filters</code> and <code>handler</code> are the only required keys, and every key inside <code>filters</code> is optional, so <code>filters: {}</code> registers a catch-all.</p><p>A route has no schema key. An authorizer is handed a token and the metadata of a request someone else is making, rather than a payload you control, so there is nothing to validate.</p><p><code>route()</code> returns the router, so you can chain registrations.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(tokenRoute).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(readRoute).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(writeRoute)</span></span></code></pre></div><p>Routes match in registration order and the first match wins, so give each route filters no other route can match. A catch-all registered first swallows every event behind it. See <a href="/lambda-event-router/docs/routing#match-order">match order</a> for what goes wrong when they overlap.</p><p><strong>When nothing matches, the router throws and the invocation fails.</strong> The message names the type and the method it could not place, and no handler runs. A failed authorizer is an error rather than a refusal, so the caller gets neither the resource nor a clean denial. See <a href="/lambda-event-router/docs/routing#nothing-matched">nothing matched</a> for what the other routers do instead.</p><h3 id="convenience-methods" tabindex="-1">Convenience methods <a class="header-anchor" href="#convenience-methods" aria-label="Permalink to &quot;Convenience methods&quot;">​</a></h3><p><code>token()</code> and <code>request()</code> fill in the <code>type</code> filter for you.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Both of these register the same route</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;GET&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseRead,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;REQUEST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;GET&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseRead,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Method</th><th>Sets</th><th>Also takes</th><th>Handler gets</th></tr></thead><tbody><tr><td><code>token()</code></td><td><code>type: &#39;TOKEN&#39;</code></td><td><code>handler</code></td><td><code>LambdaAuthorizerTokenRequest</code></td></tr><tr><td><code>request()</code></td><td><code>type: &#39;REQUEST&#39;</code></td><td><code>method</code>, <code>handler</code></td><td><code>LambdaAuthorizerRequestRequest</code></td></tr></tbody></table><p><code>request()</code>&#39;s <code>method</code> sits at the top level rather than inside <code>filters</code>, and <code>token()</code> takes a <code>handler</code> and nothing else. Neither accepts a <code>custom</code>, so a route needing one goes through <code>route()</code> or <code>defineLambdaAuthorizerRoute</code>. See <a href="/lambda-event-router/docs/routing#convenience-methods">convenience methods</a> for how the other routers use them.</p><p><strong>Both methods narrow what the handler is given, and <code>route()</code> does not.</strong> <code>route()</code> types its handler against <code>LambdaAuthorizerRequest</code>, where everything but <code>type</code>, <code>resourceArn</code>, <code>event</code> and <code>context</code> is optional, so a function annotated <code>LambdaAuthorizerTokenRequest</code> will not assign to it. <a href="#annotated-handlers">Annotated handlers</a> has the two ways round that.</p><h2 id="filters" tabindex="-1">Filters <a class="header-anchor" href="#filters" aria-label="Permalink to &quot;Filters&quot;">​</a></h2><p>Two keys match on the event, plus a <code>custom</code>. Every one of them is optional.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;REQUEST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;POST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseWrite,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Filter</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td><code>&#39;TOKEN&#39; | &#39;REQUEST&#39;</code></td><td>Which kind of authorizer sent the event. One value, not a <code>FilterStringMatcher</code>, so no array and no pattern</td></tr><tr><td><code>method</code></td><td><code>string</code></td><td>The method of the request being authorised. An exact match and case sensitive, so <code>GET</code> rather than <code>get</code></td></tr><tr><td><code>custom</code></td><td><code>(input: LambdaAuthorizerFilterInput) =&gt; boolean | Promise&lt;boolean&gt;</code></td><td>Given <code>{ type, method }</code>. Can be async</td></tr></tbody></table><p>Neither key is a <a href="/lambda-event-router/docs/routing#filters"><code>FilterStringMatcher</code></a>, so neither takes an array or a <code>RegExp</code> the way <code>bucket</code> on S3 or <code>messageAttributes</code> on SNS do. <code>type</code> has only two values to pick between, and <code>method</code> matches one method per route.</p><p><strong><code>method</code> and a <code>custom</code> reading the method do not belong in the same block.</strong> By the time the custom filter runs, <code>method</code> has already narrowed the route to one value, so anything the function adds is unreachable. Use one or the other.</p><p>Matching a group of methods is where the <code>custom</code> earns its place, since <code>{ method: &#39;POST&#39; }</code> and <code>{ method: &#39;PUT&#39; }</code> are two registrations while one function covers both.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;REQUEST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    custom</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">method</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> method </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;POST&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> method </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;PUT&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseWrite,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p><code>custom</code> gets the same two values the keys above match and nothing else, so it cannot read the token, the headers or the raw event. Filter on the type and the method, and check the rest in the handler. See <a href="/lambda-event-router/docs/routing#custom"><code>custom</code></a> for where it sits in the filter order.</p><p><strong>A <code>method</code> filter never matches a TOKEN event.</strong> A token authorizer is configured against a header rather than a route, so the event carries no method and <code>{ type: &#39;TOKEN&#39;, method: &#39;GET&#39; }</code> matches nothing at all. The same goes for a <code>custom</code> testing <code>method</code>, which is <code>undefined</code> on every TOKEN event.</p><h3 id="authorizer-types" tabindex="-1">Authorizer types <a class="header-anchor" href="#authorizer-types" aria-label="Permalink to &quot;Authorizer types&quot;">​</a></h3><p><code>type</code> is set by AWS on the event and tells you which of two contracts you are working to. A TOKEN authorizer is given a single header value and nothing about the request. A REQUEST authorizer is given the request and no token, so a bearer token there is read off <code>headers.authorization</code>.</p><table tabindex="0"><thead><tr><th>Where it runs</th><th><code>type</code></th><th><code>resourceArn</code> is</th><th>Simple response</th></tr></thead><tbody><tr><td>REST API, token source</td><td><code>TOKEN</code></td><td><code>methodArn</code></td><td>No</td></tr><tr><td>REST API, request parameters</td><td><code>REQUEST</code></td><td><code>methodArn</code></td><td>No</td></tr><tr><td>HTTP API, payload format 1.0</td><td><code>REQUEST</code></td><td><code>methodArn</code></td><td>No</td></tr><tr><td>HTTP API, payload format 2.0</td><td><code>REQUEST</code></td><td><code>routeArn</code></td><td>Yes</td></tr></tbody></table><p>The router normalises all four into one request object, so <code>resourceArn</code>, <code>method</code> and <code>path</code> read the same whichever arrived.</p><p><strong>There is no filter for the payload version.</strong> A <code>request()</code> route takes both HTTP API formats and the REST API&#39;s request authorizer, and the only difference your handler sees is whether it may answer with a boolean, which <a href="#simple-responses">Simple responses</a> covers.</p><h2 id="handler" tabindex="-1">Handler <a class="header-anchor" href="#handler" aria-label="Permalink to &quot;Handler&quot;">​</a></h2><p>Handlers take one argument and return the policy API Gateway should act on.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { APIGatewayAuthorizerResult } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aws-lambda&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { LambdaAuthorizerTokenRequest } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Allow, Deny } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> authoriseToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaAuthorizerTokenRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">APIGatewayAuthorizerResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">authorizationToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">resourceArn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fromToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(authorizationToken)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">user) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(user.id, resourceArn, { tenantId: user.tenantId })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="request-object" tabindex="-1">Request object <a class="header-anchor" href="#request-object" aria-label="Permalink to &quot;Request object&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td><code>AuthorizerType</code></td><td><code>&#39;TOKEN&#39;</code> or <code>&#39;REQUEST&#39;</code></td></tr><tr><td><code>resourceArn</code></td><td><code>string</code></td><td>The ARN of what is being called. Scope the policy you return to it</td></tr><tr><td><code>authorizationToken</code></td><td><code>string</code></td><td>The raw token source value, <code>Bearer eyJ...</code> and all. Only a TOKEN authorizer carries one</td></tr><tr><td><code>method</code></td><td><code>string</code></td><td>The method of the request being authorised. Only a REQUEST authorizer</td></tr><tr><td><code>path</code></td><td><code>string</code></td><td>The path being called. Only a REQUEST authorizer</td></tr><tr><td><code>headers</code></td><td><code>Record&lt;string, string | undefined&gt;</code></td><td>The request headers. Only a REQUEST authorizer</td></tr><tr><td><code>query</code></td><td><code>Record&lt;string, string | undefined&gt;</code></td><td>The query string parameters, <code>{}</code> when there are none. Only a REQUEST authorizer</td></tr><tr><td><code>event</code></td><td><code>LambdaAuthorizerEvent</code></td><td>The untouched event from AWS, for the request context and anything else you need</td></tr><tr><td><code>context</code></td><td><code>Context</code></td><td>The Lambda context</td></tr></tbody></table><p><code>Context</code> comes from <code>aws-lambda</code>, not from this package. <code>LambdaAuthorizerEvent</code> types <code>event</code> and is exported from here, as a union of that package&#39;s three authorizer event types.</p><p><strong>Header names arrive lowercased.</strong> The router lowercases a REST API&#39;s and an HTTP API sends them that way already, so <code>headers.authorization</code> and <code>headers[&#39;x-api-key&#39;]</code> are the spellings to read.</p><p>Which of the optional fields you actually get is fixed by the authorizer type, and the request types say so, so a handler registered through <a href="#convenience-methods"><code>token()</code> or <code>request()</code></a> reads them without a check.</p><h3 id="response-type" tabindex="-1">Response type <a class="header-anchor" href="#response-type" aria-label="Permalink to &quot;Response type&quot;">​</a></h3><p>A handler returns <code>LambdaAuthorizerResult</code>, a union of <code>APIGatewayAuthorizerResult</code> and <code>APIGatewaySimpleAuthorizerResult</code>. A <code>boolean</code> works as shorthand for the second. Both come from <code>aws-lambda</code>, and <a href="#policy-helpers"><code>Allow</code> and <code>Deny</code></a> build the first. See <a href="#responses">Responses</a> for what the router does with each.</p><h3 id="inferred-handlers" tabindex="-1">Inferred handlers <a class="header-anchor" href="#inferred-handlers" aria-label="Permalink to &quot;Inferred handlers&quot;">​</a></h3><p>Nothing to look up and nothing to keep in sync. <code>defineLambdaAuthorizerRoute</code> reads the <code>type</code> filter and hands your handler the matching request, so <code>authorizationToken</code> below is a <code>string</code> rather than <code>string | undefined</code> without you declaring anything.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Allow, defineLambdaAuthorizerRoute, Deny } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> tokenRoute</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineLambdaAuthorizerRoute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;TOKEN&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">handle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">authorizationToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">resourceArn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">scheme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authorizationToken.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">split</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39; &#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (scheme </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Bearer&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">token) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fromToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(token)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">user) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">warn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Rejected a token for \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">resourceArn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(user.id, resourceArn, { tenantId: user.tenantId })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(tokenRoute)</span></span></code></pre></div><p><strong>The <code>type</code> filter is what picks the request shape.</strong> <code>{ type: &#39;TOKEN&#39; }</code> infers <code>LambdaAuthorizerTokenRequest</code>, <code>{ type: &#39;REQUEST&#39; }</code> infers <code>LambdaAuthorizerRequestRequest</code>, and a route without a <code>type</code> infers <code>LambdaAuthorizerRequest</code> with everything optional. The convenience methods narrow the same way, since each sets the filter itself.</p><p>Inference pays off most in a Lambda taking several event sources, since you never have to know any of their request shapes. See <a href="/lambda-event-router/docs/handlers#inferred-handlers">inferred handlers</a>, where the same queue is written both ways to compare.</p><h3 id="annotated-handlers" tabindex="-1">Annotated handlers <a class="header-anchor" href="#annotated-handlers" aria-label="Permalink to &quot;Annotated handlers&quot;">​</a></h3><p>Annotating the request yourself splits route setup from business logic, using one of the three request types on <a href="#types">Types</a> and your own.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// handlers/auth.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { APIGatewayAuthorizerResult } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aws-lambda&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { LambdaAuthorizerRequestRequest } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Allow, Deny } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> authoriseRead</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaAuthorizerRequestRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">APIGatewayAuthorizerResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">headers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">method</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">resourceArn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> request</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> headers[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;x-api-key&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> client</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> apiKeys.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lookup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(key)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">warn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Rejected an API key for \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">method</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client.id, resourceArn, { plan: client.plan })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// authorizer.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createLambdaAuthorizerRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { authoriseRead } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./handlers/auth.js&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> authRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createLambdaAuthorizerRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;GET&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, handler: authoriseRead })</span></span></code></pre></div><p><strong>A narrow request type only assigns through <code>token()</code> or <code>request()</code>.</strong> Registering the same handler with <code>route()</code> is rejected, because <code>route()</code> types its handler against <code>LambdaAuthorizerRequest</code> whatever the route filters to. Use the convenience method, or annotate the handler <code>LambdaAuthorizerRequest</code> and check the optional fields yourself.</p><p>There is no schema on a route to derive a type from, so the request types are the whole surface and the only thing you write yourself is the return type. <a href="/lambda-event-router/docs/handlers#annotated-handlers">Annotated handlers</a> has the worked version.</p><h2 id="responses" tabindex="-1">Responses <a class="header-anchor" href="#responses" aria-label="Permalink to &quot;Responses&quot;">​</a></h2><p>Whatever a handler returns goes back to API Gateway as the result of the invocation. An IAM policy works everywhere, and a boolean works on one of the four authorizer configurations.</p><h3 id="policy-helpers" tabindex="-1">Policy helpers <a class="header-anchor" href="#policy-helpers" aria-label="Permalink to &quot;Policy helpers&quot;">​</a></h3><p><code>Allow</code> and <code>Deny</code> build the policy document API Gateway expects, so you pass the principal and the resource rather than writing the statement out.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Allow, Deny } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user-123&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user-123&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn, { tenantId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;acme&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, plan: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pro&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Helper</th><th>Effect</th><th>Third argument</th></tr></thead><tbody><tr><td><code>Allow(principalId, resource, context?)</code></td><td><code>Allow</code></td><td>A <code>Record&lt;string, string | number | boolean&gt;</code> passed through to your API</td></tr><tr><td><code>Deny(principalId, resource)</code></td><td><code>Deny</code></td><td>None</td></tr><tr><td><code>generatePolicy(principalId, effect, resource)</code></td><td>Whichever you pass</td><td>The resource, since the effect takes the second slot</td></tr></tbody></table><p><code>generatePolicy</code> is the one to reach for when the effect is a variable rather than a branch, and <code>Allow</code> is the only one taking a context.</p><p>Whatever you put in that context reaches the handler behind the API on <a href="/lambda-event-router/routers/APIGatewayRouter#auth"><code>request.auth</code></a>, which is how an authorizer passes a tenant id or a plan on without the API looking it up again. API Gateway passes the values through as strings, so a number goes out as <code>3</code> and arrives as <code>&#39;3&#39;</code>.</p><p><strong>Scope the resource to what you mean to allow, and remember the policy can be cached.</strong> The <code>resourceArn</code> off the request names one method on one route, so a policy built from it allows that and nothing else. Where the API caches authorizer results, the cached policy is reused for the identity&#39;s next request, and one pinned to a single ARN then refuses it. A wildcard such as <code>arn:aws:execute-api:eu-west-2:123456789012:abc123/prod/*/*</code> covers the API instead, at the cost of the policy no longer being per route.</p><p>Both helpers take a single resource string, so a policy naming several ARNs without a wildcard means building the document yourself.</p><h3 id="simple-responses" tabindex="-1">Simple responses <a class="header-anchor" href="#simple-responses" aria-label="Permalink to &quot;Simple responses&quot;">​</a></h3><p>An HTTP API request authorizer on payload format 2.0 can answer with a boolean, and the router wraps it as <code>{ isAuthorized }</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // { isAuthorized: true }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // { isAuthorized: false }</span></span></code></pre></div><p><strong>Returning a boolean from anything else throws and fails the invocation.</strong> A REST API authorizer and an HTTP API on payload format 1.0 both expect a policy, so the router refuses the boolean rather than sending something API Gateway cannot read. Nothing catches this at compile time, because the return type allows a boolean on every route.</p><p>No filter reaches the payload version, so a route covering both HTTP API formats has to return a policy. Answer with a boolean only where the authorizer is pinned to 2.0.</p><h3 id="throwing" tabindex="-1">Throwing <a class="header-anchor" href="#throwing" aria-label="Permalink to &quot;Throwing&quot;">​</a></h3><p>Throwing a policy works the same as returning it, and it carries the same weight from any depth, so a token check three calls below your handler can refuse a request without every function in between passing a failure back up.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Deny } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fromToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(token)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">user) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Deny</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;anonymous&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, resourceArn)</span></span></code></pre></div><p><strong>Only a policy is caught.</strong> The router checks a thrown value for a <code>principalId</code> and a <code>policyDocument</code>, so a thrown <code>{ isAuthorized: false }</code> is not a response and fails the invocation. Return the boolean rather than throwing it. <code>isAuthorizerResponse</code> is that check, exported so you can run it yourself.</p><p><strong>Anything else thrown fails the invocation.</strong> The router rethrows it untouched, so Lambda records the error and the caller gets an error from API Gateway rather than a refusal. A denial you mean is a <code>Deny</code>, not a <code>throw new Error(&#39;unauthorised&#39;)</code>.</p><h2 id="middleware" tabindex="-1">Middleware <a class="header-anchor" href="#middleware" aria-label="Permalink to &quot;Middleware&quot;">​</a></h2><p>Router and route middleware are both typed <code>LambdaAuthorizerMiddleware</code>, and the chain runs once per authorisation.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { LambdaAuthorizerMiddleware } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/apigateway&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withTiming</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaAuthorizerMiddleware</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">appendKeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ resourceArn: request.resourceArn })</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(request)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> authRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createLambdaAuthorizerRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ middleware: [withTiming] })</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">authRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filters: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;REQUEST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;POST&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  middleware: [withAudit],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  handler: authoriseWrite,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>Router middleware runs before route middleware, and both run before the handler. A middleware can short-circuit by returning a policy or a boolean without calling <code>next</code>, and a response thrown from inside the chain, with <code>Deny()</code> or <code>Allow()</code>, is caught the same way a throw from the handler is. See <a href="/lambda-event-router/docs/middleware">middleware</a> for the execution order and the three levels it attaches at.</p><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><p>All exported from <code>@lambda-event-router/apigateway</code>.</p><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>LambdaAuthorizerRequest</code></td><td>The handler argument, with every field only one authorizer type carries optional</td></tr><tr><td><code>LambdaAuthorizerTokenRequest</code></td><td>The same for a TOKEN authorizer, with <code>authorizationToken</code> required</td></tr><tr><td><code>LambdaAuthorizerRequestRequest</code></td><td>The same for a REQUEST authorizer, with <code>method</code>, <code>path</code>, <code>headers</code> and <code>query</code> required</td></tr><tr><td><code>LambdaAuthorizerBaseRequest</code></td><td>The four fields all three share</td></tr><tr><td><code>LambdaAuthorizerResult</code></td><td>What the router hands back to Lambda</td></tr><tr><td><code>LambdaAuthorizerHandler</code></td><td>A handler, as <code>route()</code> types it</td></tr><tr><td><code>LambdaAuthorizerMiddleware</code></td><td>Router and route middleware</td></tr><tr><td><code>LambdaAuthorizerRouteDefinition</code></td><td>A full route, as <code>defineLambdaAuthorizerRoute</code> builds it</td></tr><tr><td><code>LambdaAuthorizerRouterOptions</code></td><td>Options for <code>createLambdaAuthorizerRouter</code></td></tr><tr><td><code>LambdaAuthorizerTokenInput</code></td><td>The argument to <code>token()</code></td></tr><tr><td><code>LambdaAuthorizerRequestInput</code></td><td>The argument to <code>request()</code></td></tr><tr><td><code>LambdaAuthorizerFilters</code></td><td>The <code>filters</code> key</td></tr><tr><td><code>LambdaAuthorizerFilterInput</code></td><td>What a <code>custom</code> is given</td></tr><tr><td><code>LambdaAuthorizerEvent</code></td><td><code>request.event</code></td></tr><tr><td><code>AuthorizerType</code></td><td><code>&#39;TOKEN&#39; | &#39;REQUEST&#39;</code></td></tr></tbody></table><p><code>Context</code> on the request comes from <code>aws-lambda</code>, and so do the three events <code>LambdaAuthorizerEvent</code> unions together and both halves of <code>LambdaAuthorizerResult</code>. <code>Allow</code>, <code>Deny</code> and <code>generatePolicy</code> all return that package&#39;s <code>APIGatewayAuthorizerResult</code>, which is what to annotate a handler&#39;s return type with.</p><p>None of these takes a generic parameter. A route carries no schema, so there is nothing for a type to pass through, and the request shape comes from the <code>type</code> filter instead.</p><p>The <code>LambdaAuthorizerRouter</code> class and the <code>createLambdaAuthorizerRouter</code> and <code>defineLambdaAuthorizerRoute</code> functions come from the same place, along with <code>Allow</code>, <code>Deny</code>, <code>generatePolicy</code> and <code>isAuthorizerResponse</code>.</p><h2 id="code-example" tabindex="-1">Code example <a class="header-anchor" href="#code-example" aria-label="Permalink to &quot;Code example&quot;">​</a></h2><p>One Lambda backing three authorizers on the same API: a token authorizer on the REST API, and request authorizers for reads and for writes.</p><p>Open a file: <a href="#authorizer-example:index.ts">index.ts</a> | <a href="#authorizer-example:authorizer.ts">authorizer router</a> | <a href="#authorizer-example:handlers/auth.ts">handlers</a> | <a href="#authorizer-example:accounts.ts">lookups</a></p>`,93)),r(i,{files:a,id:"authorizer-example","default-file":"authorizer.ts","line-numbers":"","collapse-toggle":"","fixed-height":""}),s[1]||(s[1]=e('<p>Every route carries a filter no other route can match, so the order they are registered in makes no difference. <code>GET</code> is picked by the <code>method</code> filter and every other method by the <code>custom</code> beside it, which is the one thing a single <code>method</code> key cannot express.</p><p>All three handlers answer with a policy rather than a boolean. A route cannot filter on the payload version, so a boolean would fail on anything but an HTTP API pinned to 2.0.</p><p><code>authoriseRead</code> and <code>authoriseWrite</code> reach their narrow request type two different ways: <code>request()</code> narrows it for you, and <code>defineLambdaAuthorizerRoute</code> infers it from the <code>type</code> filter. Handing either handler to <code>route()</code> instead would not compile.</p><p><code>index.ts</code> hands the router to <code>LambdaRouter</code>, which is what AWS invokes and what every router in the Lambda gets registered on. See <a href="/lambda-event-router/docs/routers">routers</a> for how the two levels of matching fit together.</p>',4))])}}});export{k as __pageData,c as default};
