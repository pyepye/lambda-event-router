import{C as n,o as r,c as o,ak as h,E as d,j as s,a}from"./chunks/framework.DiNuf6zK.js";const u=JSON.parse('{"title":"LambdaRouter","description":"","frontmatter":{},"headers":[],"relativePath":"routers/LambdaRouter.md","filePath":"routers/LambdaRouter.md"}'),l={name:"routers/LambdaRouter.md"},g=Object.assign(l,{setup(p){const t=[{path:"index.ts",code:`import type { Handler } from 'aws-lambda'
import { LambdaRouter } from '@lambda-event-router/base'

import { apiRouter } from './api.js'
import { eventRouter } from './events.js'
import { withTiming } from './middleware/withTiming.js'
import { sqsRouter } from './sqs.js'

// eventRouter is sorted last whatever order it is written in, so the other two get first refusal
const lambdaRouter = new LambdaRouter({
  routers: [apiRouter, sqsRouter, eventRouter],
  middleware: [withTiming],
})

export const handler: Handler = lambdaRouter.handler()`},{path:"api.ts",code:`import { createAPIGatewayRouter } from '@lambda-event-router/apigateway'

import { getOrder } from './handlers/orders.js'

export const apiRouter = createAPIGatewayRouter()

apiRouter.get({
  filters: { path: '/orders/:orderId' },
  handler: getOrder,
})`},{path:"sqs.ts",code:`import { createSQSRouter } from '@lambda-event-router/sqs'

import { processOrder } from './handlers/orders.js'

const ORDER_QUEUE_ARN = 'arn:aws:sqs:eu-west-2:123456789012:orders'

export const sqsRouter = createSQSRouter({ batchItemFailures: true })

sqsRouter.route({
  filters: { eventSourceArn: ORDER_QUEUE_ARN },
  handler: processOrder,
})`},{path:"events.ts",code:`import { createEventRouter, defineEventRoute, isObject, logger } from '@lambda-event-router/base'
import { z } from 'zod'

export const eventRouter = createEventRouter()

// EventBridge Scheduler sends the payload you configured, so there is no envelope to filter on
const reportRoute = defineEventRoute({
  filters: {
    custom: ({ event }) => isObject(event) && event.command === 'generate-report',
  },
  eventSchema: z.object({
    command: z.literal('generate-report'),
    day: z.iso.date(),
  }),
}).handle(async ({ event }) => {
  logger.info(\`Building the order report for \${event.day}\`)

  return { day: event.day }
})

eventRouter.route(reportRoute)`},{path:"middleware/withTiming.ts",code:`import { logger, type LambdaMiddleware } from '@lambda-event-router/base'

// Global middleware gets the raw event and the Lambda context
export const withTiming: LambdaMiddleware = async (event, context, next) => {
  const startedAt = Date.now()
  logger.appendKeys({ requestId: context.awsRequestId })

  try {
    return await next(event, context)
  } finally {
    logger.info(\`Invocation finished in \${Date.now() - startedAt}ms\`)
  }
}`},{path:"handlers/orders.ts",code:`import type { ApiRequest, HandlerResponse } from '@lambda-event-router/apigateway'
import { logger } from '@lambda-event-router/base'
import type { SQSRequest, SQSResponse } from '@lambda-event-router/sqs'

import { type Order, orders } from '../services/orders.js'

export async function getOrder(
  request: ApiRequest<{ orderId: string }>,
): Promise<HandlerResponse<Order>> {
  return orders.get(request.path.orderId)
}

export async function processOrder(request: SQSRequest<Order>): Promise<SQSResponse> {
  logger.info(\`Processing order \${request.body.orderId} from the queue\`)
  await orders.process(request.body)
}`}];return(k,e)=>{const i=n("CodeFileViewer");return r(),o("div",null,[e[0]||(e[0]=h(`<h1 id="lambdarouter" tabindex="-1">LambdaRouter <a class="header-anchor" href="#lambdarouter" aria-label="Permalink to &quot;LambdaRouter&quot;">​</a></h1><p><code>LambdaRouter</code> is the entry point. It takes every router in your Lambda and works out which one owns each event before any of your filters run.</p><p>You export what <code>handler()</code> gives you, so AWS invokes <code>LambdaRouter</code> and <code>LambdaRouter</code> invokes everything else. It has no routes, no filters and no schemas of its own, because those belong to the service routers you register on it.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @lambda-event-router/base</span></span></code></pre></div><p>Every service package pulls <code>base</code> in, so it may already be in your tree. Install it by name anyway, since a package you import from belongs in your own <code>package.json</code>.</p><h2 id="create-the-router" tabindex="-1">Create the router <a class="header-anchor" href="#create-the-router" aria-label="Permalink to &quot;Create the router&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Handler } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aws-lambda&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { LambdaRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { apiRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./api.js&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { withTiming } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./middleware/withTiming.js&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sqsRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./sqs.js&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lambdaRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  routers: [apiRouter, sqsRouter],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  middleware: [withTiming],  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lambdaRouter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p><code>routers</code> is the only required option.</p><p><code>createLambdaRouter({ routers: [apiRouter, sqsRouter] })</code> is the same call in the factory form the service routers use, so pick whichever reads better to you.</p><h3 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Required</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>routers</code></td><td><code>EventTypeRouter[]</code></td><td>Yes</td><td></td><td>Every router this Lambda can receive events for. See <a href="#picking-a-router">Picking a router</a></td></tr><tr><td><code>middleware</code></td><td><code>LambdaMiddleware[]</code></td><td>No</td><td><code>[]</code></td><td>Runs for every event, whichever router takes it. See <a href="#middleware">Middleware</a></td></tr></tbody></table><p>The options object is typed <code>LambdaRouterOptions</code> and a global middleware function is typed <code>LambdaMiddleware</code>, both exported from <code>@lambda-event-router/base</code>.</p><h2 id="export-the-handler" tabindex="-1">Export the handler <a class="header-anchor" href="#export-the-handler" aria-label="Permalink to &quot;Export the handler&quot;">​</a></h2><p><code>handler()</code> returns an <code>aws-lambda</code> <code>Handler</code>. Call it once at module scope and export the result, which is the function you point your Lambda&#39;s handler setting at.</p><p>Whatever the matched router returned comes back untouched. An HTTP router&#39;s status code and body reach API Gateway as they are, an SQS router reporting batch item failures returns those, and a router whose handlers return nothing resolves to <code>undefined</code>.</p><h2 id="picking-a-router" tabindex="-1">Picking a router <a class="header-anchor" href="#picking-a-router" aria-label="Permalink to &quot;Picking a router&quot;">​</a></h2><p><code>LambdaRouter</code> asks each router in turn whether it recognises the event, through the <code>canHandleEvent</code> method every router has, and gives the event to the first one that says yes.</p><p>Each answer is awaited before the next router is asked, so a router can decide asynchronously. <code>EventRouter</code> does, because it runs your <code>custom</code> to make up its mind.</p><p>None of that is yours to write. Each router recognises its own event source from the shape AWS sends, so adding a second router cannot change how the first one&#39;s events are routed.</p><h3 id="claiming-an-event-and-then-missing" tabindex="-1">Claiming an event and then missing <a class="header-anchor" href="#claiming-an-event-and-then-missing" aria-label="Permalink to &quot;Claiming an event and then missing&quot;">​</a></h3><p>A router that claims an event and then finds none of its own routes match throws <code>NoRouteMatchedError</code>, and <code>LambdaRouter</code> carries on down the list rather than failing. That is what lets <code>EventRouter</code> and <code>StepFunctionsRouter</code> sit on one Lambda: a custom JSON payload gives neither of them a shape to recognise, so their routes settle which one owns an event and the order you register in does not.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Either order works. The TaskToken payload matches a route on one and not the other</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lambdaRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  routers: [stepFunctionsRouter, eventRouter],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p><strong>Only that error falls through.</strong> Anything else fails the invocation, a failing <code>eventSchema</code> included, so a malformed payload cannot end up handled by a router that was never meant to see it.</p><h3 id="one-router-per-event-source" tabindex="-1">One router per event source <a class="header-anchor" href="#one-router-per-event-source" aria-label="Permalink to &quot;One router per event source&quot;">​</a></h3><p>Put every route for one event source on one router. The HTTP and record routers do not fall through, so a second instance of one never receives anything: the first claims the event, then answers its own 404 or throws.</p><p><strong>So splitting an API across two <code>APIGatewayRouter</code> instances does not work.</strong> Both recognise every request, the first one takes them all, and a path only the second has a route for comes back as the first router&#39;s 404 rather than reaching it.</p><h3 id="catch-all-routers" tabindex="-1">Catch-all routers <a class="header-anchor" href="#catch-all-routers" aria-label="Permalink to &quot;Catch-all routers&quot;">​</a></h3><p><code>EventRouter</code> claims an event when one of its own routes matches, so a <code>custom</code> returning <code>true</code> for anything claims everything. <code>LambdaRouter</code> sorts <code>EventRouter</code> to the end of the list whatever order you registered in, which gives every dedicated router first refusal on its own events.</p><p><code>StepFunctionsRouter</code> is a catch-all too, claiming any object it does not recognise as one of a short list of AWS event shapes, far fewer than <code>EventRouter</code> knows about. <code>LambdaRouter</code> sorts it after the dedicated routers as well, and ahead of <code>EventRouter</code>. A shape it claims but has no route for throws <code>NoRouteMatchedError</code>, which falls through to <code>EventRouter</code> as the final fallback.</p><h2 id="no-router-for-the-event" tabindex="-1">No router for the event <a class="header-anchor" href="#no-router-for-the-event" aria-label="Permalink to &quot;No router for the event&quot;">​</a></h2><p>An event no router recognises throws <code>No router found for event</code>, which fails the invocation. This happens before any route matching, so no filter of yours has run and nothing has been validated.</p><p>An event that was claimed and then matched no route fails with the last claiming router&#39;s own error instead, so the message names the router that got closest.</p><p>Reaching the right router is not the same as finding a route on it, and the two fail differently. A request <code>APIGatewayRouter</code> recognises and has no route for is a 404 from that router, and an SQS record with no matching route throws inside <code>SQSRouter</code>.</p><p>See <a href="/docs/routing#nothing-matched">nothing matched</a> for what each router does with an event it owns.</p><p><code>EventRouter</code> counts as not recognising the event when none of its routes match, so a custom payload that misses every <code>custom</code> fails here rather than inside <code>EventRouter</code>.</p><h2 id="middleware" tabindex="-1">Middleware <a class="header-anchor" href="#middleware" aria-label="Permalink to &quot;Middleware&quot;">​</a></h2><p>Middleware passed to <code>LambdaRouter</code> runs for every event the Lambda receives, whichever router ends up taking it, so tracing, timing and per-invocation logger keys only need writing once.</p><p><strong>It has a different signature to router and route middleware.</strong> No router has been picked yet, so there is no request object to hand you and you get the raw event and the Lambda context instead. The signature is exported as <code>LambdaMiddleware</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { logger, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LambdaMiddleware } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> withTiming</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaMiddleware</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> startedAt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Date.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">appendKeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ requestId: context.awsRequestId })</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event, context)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Invocation finished in \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> startedAt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}ms\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Because it wraps the routing rather than sitting inside it, a <code>try</code> / <code>catch</code> around <code>await next()</code> also catches <code>No router found for event</code>, which is the only way to answer an event nothing recognised.</p><p><strong>Calling <code>next()</code> twice routes and handles the event twice instead of throwing.</strong> The guard against that lives in the router and route chain, not this one.</p><p>See <a href="/docs/middleware">middleware</a> for the execution order and the three levels it attaches at.</p><h2 id="per-invocation-logger-keys" tabindex="-1">Per-invocation logger keys <a class="header-anchor" href="#per-invocation-logger-keys" aria-label="Permalink to &quot;Per-invocation logger keys&quot;">​</a></h2><p><code>LambdaRouter</code> calls <code>resetKeys()</code> on the active logger at the start of every invocation, before your middleware runs. Temporary keys added with <code>appendKeys()</code> cannot leak into the next event a warm container receives.</p><p>Keys you want to survive the reset go on with <code>appendPersistentKeys()</code>. See <a href="/docs/logging#per-invocation-keys">logging</a> for the split between the two.</p><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><p>Exported from <code>@lambda-event-router/base</code>.</p><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>EventTypeRouter&lt;TEvent, TResult&gt;</code></td><td>What every router satisfies, and what the <code>routers</code> array holds</td></tr><tr><td><code>LambdaRouterOptions</code></td><td>Options for <code>new LambdaRouter</code> and <code>createLambdaRouter</code></td></tr><tr><td><code>LambdaMiddleware</code></td><td>A global middleware function, <code>(event, context, next) =&gt; Promise&lt;unknown&gt;</code></td></tr></tbody></table><p>The <code>LambdaRouter</code> class and the <code>createLambdaRouter</code> function come from the same place. <code>Handler</code> and <code>Context</code> come from <code>aws-lambda</code>, not from this package.</p><h3 id="generic-parameters" tabindex="-1">Generic parameters <a class="header-anchor" href="#generic-parameters" aria-label="Permalink to &quot;Generic parameters&quot;">​</a></h3><p><code>EventTypeRouter</code> takes two, and both default to <code>unknown</code>.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Types</th><th>Default</th></tr></thead><tbody><tr><td><code>TEvent</code></td><td>The event <code>handleEvent</code> is given</td><td><code>unknown</code></td></tr><tr><td><code>TResult</code></td><td>What <code>handleEvent</code> resolves to</td><td><code>unknown</code></td></tr></tbody></table><p><code>routers</code> is typed <code>EventTypeRouter[]</code>, so it takes the bare form with both left off. You only need the parameters when you are writing a router of your own.</p><h2 id="custom-routers" tabindex="-1">Custom routers <a class="header-anchor" href="#custom-routers" aria-label="Permalink to &quot;Custom routers&quot;">​</a></h2><p>Anything satisfying <code>EventTypeRouter</code> can go in the <code>routers</code> array, so a service with no router of its own is two methods rather than a fork of the library.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Context } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aws-lambda&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { EventTypeRouter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { isObject, LambdaRouter, logger } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@lambda-event-router/base&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> JobEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  jobName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> jobRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  canHandleEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> isObject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.jobName </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  handleEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> JobEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ran</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Running job \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">jobName</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} for request \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">context</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">awsRequestId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ran: event.jobName }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">satisfies</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EventTypeRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">JobEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ran</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> lambdaRouter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LambdaRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ routers: [sqsRouter, jobRouter] })</span></span></code></pre></div><p><code>canHandleEvent</code> may return a promise, and narrowing the event with <code>isObject</code> from <code>@lambda-event-router/base</code> is what keeps it honest about the <code>unknown</code> it is given.</p><p><strong>Recognise as little as you can get away with.</strong> A <code>canHandleEvent</code> that returns <code>true</code> too readily takes events a dedicated router would have handled, and the only sign is a handler receiving something it did not expect. <code>EventRouter</code> covers a custom payload with filters and schema validation already, so reach for a router of your own when you need the whole event source rather than one payload shape.</p><p>A router that recognises an event loosely can throw <code>NoRouteMatchedError</code>, exported from <code>@lambda-event-router/base</code>, from <code>handleEvent</code> to hand the event back, and <code>LambdaRouter</code> tries the next router. That is the fall-through <code>EventRouter</code> and <code>StepFunctionsRouter</code> rely on, and throwing it is how a custom router opts in.</p><h2 id="code-example" tabindex="-1">Code example <a class="header-anchor" href="#code-example" aria-label="Permalink to &quot;Code example&quot;">​</a></h2><p>An orders Lambda taking API Gateway requests, SQS messages and a scheduled report payload, with one middleware timing all three.</p><p>Open a file: <a href="#lambda-example:index.ts">index.ts</a> | <a href="#lambda-example:api.ts">API router</a> | <a href="#lambda-example:sqs.ts">SQS router</a> | <a href="#lambda-example:events.ts">event router</a> | <a href="#lambda-example:middleware/withTiming.ts">middleware</a> | <a href="#lambda-example:handlers/orders.ts">handlers</a></p>`,63)),d(i,{files:t,id:"lambda-example","default-file":"index.ts","line-numbers":"","collapse-toggle":"","fixed-height":""}),e[1]||(e[1]=s("p",null,[a("Three event sources, one exported handler and one place that knows about all of them. Each router recognises its own events, so nothing in "),s("code",null,"index.ts"),a(" inspects the event and adding a fourth source changes nothing about the first three.")],-1)),e[2]||(e[2]=s("p",null,[s("code",null,"withTiming"),a(" covers all three, because it sits above the router that ends up taking the event. See "),s("a",{href:"/docs/middleware"},"middleware"),a(" for the execution order and the three levels it attaches at.")],-1))])}}});export{u as __pageData,g as default};
